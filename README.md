# veliuysal/homebrew-tap

Homebrew tap for [arc-dev](https://github.com/veliuysal/arc-dev).

## Install

```sh
brew tap veliuysal/tap
brew trust veliuysal/tap
brew install arc-dev
```

## Update

Release automation updates `Formula/arc-dev.rb` when a new `v*` tag is published.
